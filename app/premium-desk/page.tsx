import { PremiumDeskShell } from "@/components/premium-desk-shell";

export const metadata = {
  title: "Premium Desk | TradeStrix",
  description:
    "Engine 26 Optimal Premium Harvest Timing — read-only option premium selling analyzer with VRP, regime gating and fair-value checks.",
};

export default function PremiumDeskPage() {
  return <PremiumDeskShell />;
}
