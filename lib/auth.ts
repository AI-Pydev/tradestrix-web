/**
 * Strangler Fig Facade for Authentication and Admin User Management.
 * Re-exports everything from the modular `@/modules/auth` package for backward compatibility.
 */

export * from "@/modules/auth";
